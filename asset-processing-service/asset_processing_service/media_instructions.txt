
UPDATE  function update_asset_content inside api_client.py to pass token_count to a PATCH request made inside update_asset_content function. 

We will USE ticktoken to encode the content : str passed in as parameter to tokens using the tiktoken library as illustated below: 


# To get the tokeniser corresponding to a specific model in the OpenAI API:
encoding = tiktoken.encoding_for_model("gpt-4o")

# To encode a string:
tokens = encoding.encode(content)

token_count = len(tokens)

UPDATE the json to include =`{"content": content , "token_count": token_count} 

and pass it to the PATCH request inside update_asset_content function. 

UPDATE any other code required to make sure token_count gets updated in the asset table defined in "schema.ts" file 




